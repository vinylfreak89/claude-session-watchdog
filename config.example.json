{
  "_comment": "Copy to config.json (gitignored). Selectors are a session id (local_...), a cli session id, or a unique title substring.",
  "target": "<title substring or session id of the session to watch>",
  "self": "<this watchdog session's own id, from get_session self>",
  "repo": null,
  "ledger": null,
  "other_ref": null,
  "perm_paths": [],
  "row_pattern": "[A-Z]{1,2}\\d{1,3}",
  "quiet_min": 10,
  "stale_after_s": 1800,
  "stall_min": 20,
  "reply_min": 20,
  "dead_min": 10,
  "codex_quiet_min": 30,
  "stale_turns": 5
}
