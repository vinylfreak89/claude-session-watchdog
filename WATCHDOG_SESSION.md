# Watchdog session — standing instruction

The standing instruction is the skill at `skills/watchdog/SKILL.md` (install: `ln -s <repo>/skills/watchdog
~/.claude/skills/watchdog`, then `/watchdog` in the session that runs the loop). One source of truth; nothing
here restates it.
